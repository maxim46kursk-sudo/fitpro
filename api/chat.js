import { createClient } from '@supabase/supabase-js'
import { effectiveLevel, AI_MIN_LEVEL } from './_access.js'
import { rateLimit } from './_ratelimit.js'
// Журнал ошибок + мгновенное уведомление тренеру. Файл с подчёркиванием —
// не serverless-функция.
import { reportError } from './_logError.js'
// Разбор ответа модели про этикетку — общий код с api/set-exercise.js, где
// живёт справочник food_products. Пределы правдоподобия должны совпадать в
// обеих ручках. Файл с подчёркиванием — не serverless-функция.
import {
  parseModelJson, normalizeLabelProduct, isValidBarcode, isSoftSource,
  cleanSourceLink, sanitizeMacros, hasUsableMacros, fromRow,
} from './_foodProduct.js'

// Потолок времени выполнения функции. Без него Vercel рубит ответ по короткому
// дефолту, и длинные ответы модели просто обрываются на полуслове. 60 секунд —
// максимум плана Hobby.
export const maxDuration = 60

// Серверный клиент Supabase — проверка токена (auth.getUser) и, ниже, чтение
// пакета пользователя service_role-ключом. Тот же env и те же безопасные
// fallback-значения (URL и publishable-ключ несекретны), что и у клиента
// (src/supabase.js) — без fallback createClient падает сразу при холодном
// старте, если переменная почему-то не долетела до серверной функции.
const SUPABASE_URL = process.env.VITE_SUPABASE_URL || 'https://api.fitproapp.ru'
const SUPABASE_KEY = process.env.VITE_SUPABASE_KEY || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzg0NzE0NTM5LCJleHAiOjE5NDIzOTQ1Mzl9.fKJZOQkyBX7sa0n0lbJ7xxGRsn5hcEyaX5ijl9P5404'
const supabase = createClient(SUPABASE_URL, SUPABASE_KEY)

// Модель и потолок длины ответа задаём здесь, а не берём из тела запроса —
// иначе анонимный клиент мог бы заказать любую доступную по ключу модель
// или огромный max_tokens за наш счёт. 4096 — с запасом над тем, что реально
// шлёт клиент (3000 для тренировок, 1000 для питания, см. AIAssistant.jsx).
const MODEL = 'claude-sonnet-4-6'
const MAX_TOKENS_CEILING = 4096

// На 5 секунд меньше maxDuration — чтобы успеть отдать клиенту осмысленный 503,
// а не быть убитыми платформой посреди ответа.
const ANTHROPIC_TIMEOUT_MS = 55_000

// ══════════════════════════════════════════════════════════════════════════
// РЕЖИМ type:'food_label' — распознавание этикетки продукта по фотографии.
//
// Зачем: сканер штрих-кода (src/BarcodeScanner.jsx) упирается в то, что в Open
// Food Facts нет доброй половины российских товаров. Вместо тупика «продукт не
// найден» предлагаем сфотографировать упаковку: модель читает таблицу
// пищевой ценности, человек проверяет числа глазами и подтверждает — карточка
// уходит в ОБЩИЙ справочник food_products (api/set-exercise.js,
// ?action=save-product), и следующему она достанется уже по одному скану.
//
// Режим живёт здесь, а не в своей ручке, по той же причине, что и ветки
// справочника в set-exercise.js: лимит 12 serverless-функций Vercel Hobby
// выбран целиком. Доступ, гейт по пакету и дневной лимит — ОБЩИЕ с обычным
// чатом, специально не отдельные: это тот же расход нашего ключа Anthropic.
// ══════════════════════════════════════════════════════════════════════════

// Потолок размера фото в base64. 1.5 МБ base64 — это примерно 1.1 МБ
// исходного jpeg, чего с запасом хватает на кадр 1280px/q0.8, который шлёт
// клиент. Больше — почти наверняка несжатый оригинал с камеры на 4–8 МБ: он и
// в лимит тела Vercel (4.5 МБ) упрётся, и денег за токены стоит заметно
// дороже без выигрыша в читаемости.
const LABEL_MAX_BASE64 = 1_500_000

// Квоты на распознавание. Режим открыт ВСЕМ вошедшим (в отличие от чата,
// который живёт с ПРОФИТа) — смысл затеи в том, чтобы общий справочник
// продуктов наполняли все, а пользуются им тоже все. Но платит за vision-запрос
// наш ключ Anthropic, поэтому бесплатному тарифу — узкая суточная квота, а
// платному — широкая почасовая.
//
// Разные механизмы под разные горизонты, и это не случайность:
//  • СУТОЧНЫЕ квоты обоих тарифов живут в Postgres (incr_feature_usage), потому
//    что в памяти инстанса они бы обнулялись на каждом холодном старте;
//  • почасовой потолок платных — in-memory _ratelimit.js: он гасит всплеск
//    «зажал кнопку», а точный учёт там не нужен.
//
// Зачем платным ЕЩЁ и суточный потолок поверх почасового: 20 в час — это до
// 480 снимков в сутки, а почасовой счётчик вдобавок мягче номинала (своя Map у
// каждого живого инстанса). Сотня в день — страховка от того, что один аккаунт
// за ночь выпишет нам счёт за vision-запросы; живой сценарий «разбираю
// покупки» до неё не дотягивается и близко.
const FREE_LABELS_PER_DAY = 3
const PAID_LABELS_PER_HOUR = 20
const PAID_LABELS_PER_DAY = 100

// Служебный домен тестового бота прогонов и его потолки. Полный прогон по
// набору товаров — это несколько десятков распознаваний подряд, и почасовые 20
// растягивают его на часы ожидания в 429.
//
// На живых пользователей это не влияет физически: домен наш, почты на нём
// заводит только qa/bot.mjs, зарегистрироваться на него снаружи нельзя.
const QA_DOMAIN = 'qa.fitproapp.ru'
const BOT_LABELS_PER_HOUR = 120
const BOT_LABELS_PER_DAY = 400

// Признак в теле 429, по которому клиент выбирает формулировку. Три разных
// исчерпания — три разных совета, и путать их нельзя:
//  • free_daily_limit — бесплатному раньше завтра ничего не поможет, кроме
//    ПРОФИТа, и сказать надо именно это;
//  • daily_limit — платному тоже ждать до завтра, но продавать ему нечего;
//  • признака нет вовсе — упёрлись в почасовой потолок, через час отпустит.
const REASON_FREE_DAILY = 'free_daily_limit'
const REASON_DAILY = 'daily_limit'

// Ответ короткий и строго структурный — потолок токенов маленький.
const LABEL_MAX_TOKENS = 512

// С поиском потолок другой: по дороге модель формулирует запросы и коротко
// комментирует найденное, и это тоже выходные токены. 1500 с запасом покрывает
// три поиска и финальный JSON; упрись мы в 512, ответ оборвался бы посреди
// поиска и пришёл вообще без JSON.
const LABEL_MAX_TOKENS_SEARCH = 1500

// ── Поиск КБЖУ в интернете ────────────────────────────────────────────────
//
// Серверный инструмент Anthropic: запросы выполняет он сам, нам не нужны ни
// своя ручка (лимит 12 функций Vercel Hobby выбран целиком), ни парсинг чужих
// сайтов.
//
// Зачем: у половины российских товаров нет карточки в Open Food Facts, а
// таблицу КБЖУ на упаковке далеко не всегда видно на снимке — мелкий шрифт,
// блик, сгиб, другая сторона пачки. Раньше в этом случае модель давала
// ТИПИЧНЫЕ числа для категории, и они расходились с реальными («майонез 67%» —
// 627 ккал вместо 610, белок 2 г вместо 0.4). В карточке товара интернет-
// магазина лежит та же таблица, что на упаковке, только читаемая.
//
// Белый список доменов. Держим здесь отдельной константой, чтобы правился он
// одной строкой: состав магазинов меняется чаще, чем всё остальное в файле.
// Критерий отбора — не популярность, а то, что КБЖУ в карточке берут из
// данных производителя. Агрегаторов «калорийность продуктов» тут нет
// намеренно: там усреднённая категория, ради ухода от которой всё и затеяно.
const SEARCH_DOMAINS = [
  'ozon.ru',
  'vkusvill.ru',
  '5ka.ru',
  'samokat.ru',
  'perekrestok.ru',
  'magnit.ru',
  'lenta.com',
  'sbermarket.ru',
  'metro-cc.ru',
]

// ══════════════════════════════════════════════════════════════════════════
// ⚙ ВЫКЛЮЧАТЕЛЬ ВЕБ-ПОИСКА. Поменять на true — поиск вернётся целиком, больше
// ничего трогать не нужно: код шага 2, белый список доменов, проверка ссылки,
// счётчик расхода и тесты на месте и рабочие.
//
// ПОЧЕМУ ВЫКЛЮЧЕН (замеры на живых запросах 2026-08-08, не оценки):
//
//  • ЦЕНА. Сам поиск стоит $10 за 1000 запросов — копейка за штуку. Дорого не
//    это: НАЙДЕННОЕ приезжает в контекст входными токенами. Замер (зефир, два
//    поиска): 22 794 входных токена — ~$0.068 против ~$0.02 за сами поиски.
//    Снимок с поиском ≈ $0.095 ≈ 8 ₽ против ≈ 1.5 ₽ без него. При квоте
//    PAID_LABELS_PER_DAY=100 это до ~760 ₽ в сутки с одного платного аккаунта.
//
//  • ВРЕМЯ. 10–15 с против 2–3 с. Человек стоит у полки с телефоном в руке.
//
//  • ТОЧНОСТЬ НЕ ТА, ЧТОБЫ ЭТО ОКУПИТЬ. Карточка магазина — про товар с тем же
//    названием, а не про пачку в руках: на том же зефире Ozon дал 322 ккал при
//    330 на упаковке. Сверять с этикеткой всё равно приходится, а раз так —
//    честная прикидка плюс просьба сверить обходится в 5 раз дешевле и втрое
//    быстрее, давая тот же итог.
//
// Вернуть имеет смысл, если поменяется экономика: подешевеют входные токены,
// появится способ получать из поиска только числа без страницы целиком, или
// найдётся источник, чьи данные можно не сверять.
// ══════════════════════════════════════════════════════════════════════════
const LABEL_WEB_SEARCH_ENABLED = false

// Тестовая лазейка, и только она: обе ветки должны остаться покрытыми и после
// отключения, иначе код поиска сгниёт незамеченным до того дня, когда его
// решат вернуть. В бою этой переменной нет — решает константа выше.
const webSearchOn = () => LABEL_WEB_SEARCH_ENABLED || process.env.LABEL_WEB_SEARCH === 'on'

// Потолок числа запросов внутри одного поиска.
const LABEL_SEARCH_MAX_USES = 3

// Инструмент включаем ТОЛЬКО платным (см. гейт в handler): поиск — наш прямой
// расход поверх и без того платного vision-запроса.
//
// user_location — не косметика: без него выдача уезжает в англоязычную, а
// «Зефир Шармэль» ни в одном англоязычном магазине не лежит. type:'approximate'
// — единственный поддерживаемый вид; страна в ISO-3166-1 alpha-2.
const LABEL_TOOLS = [{
  type: 'web_search_20250305',
  name: 'web_search',
  max_uses: LABEL_SEARCH_MAX_USES,
  allowed_domains: SEARCH_DOMAINS,
  user_location: { type: 'approximate', country: 'RU', timezone: 'Europe/Moscow' },
}]

// Ключ суточного счётчика feature_usage для поисков — ОТДЕЛЬНЫЙ от 'food_label'.
// Тот же механизм, что у фото (атомарный incr_feature_usage), но своя строка:
// иначе поиск съедал бы квоту снимков, и человек, которому не повезло с
// нечитаемыми этикетками, упирался бы в лимит фото вдвое быстрее.
const USAGE_KEY_SEARCH = 'food_label_web'

// ── ДВА ШАГА, А НЕ ОДИН ЗАПРОС С ИНСТРУМЕНТОМ ─────────────────────────────
//
// Сначала я дал модели фото И поиск в одном запросе. Живая проверка на пустом
// белом кадре 1×1 показала, почему так нельзя:
//   • без инструмента модель честно отвечает readable=false (проверено, всегда);
//   • с инструментом на том же пустом кадре она дважды выдумала товар — сперва
//     «Шармэль зефир ванильный», потом, после ужесточения промта, вообще другой,
//     «Напиток миндальный Alpro», — и оба раза приложила настоящую ссылку на
//     магазин. То есть человек получил бы уверенную карточку чужого продукта с
//     проверяемым на вид источником.
// Двумя правками промта («бери название только с фото», «пустой кадр — не повод
// угадать») это НЕ лечится: наличие инструмента само по себе толкает модель
// искать хоть что-нибудь вместо признания, что она ничего не видит.
//
// Поэтому шаги разделены, и разрыв структурный, а не текстовый:
//   ШАГ 1 — зрение, БЕЗ инструментов. Ровно тот запрос, что работал до всей
//           этой затеи. Он и решает, есть ли на фото продукт вообще.
//   ШАГ 2 — поиск, БЕЗ картинки, только по названию и марке, ПРОЧИТАННЫМ на
//           шаге 1. Выдумать товар тут нечего: если шаг 1 названия не дал,
//           шага 2 просто не происходит.
// Заодно второй запрос не тащит изображение и стоит дешевле.

// Промт задан здесь, а не берётся из тела запроса: клиент не должен иметь
// возможности подменить инструкцию и превратить режим в дешёвый произвольный
// vision-прокси. Ничего из запроса сюда не подставляется вовсе.
const LABEL_PROMPT = `На фото — упаковка продукта питания. Это может быть ЛЮБАЯ сторона упаковки: лицевая с названием или обратная с таблицей пищевой ценности.

Ответь СТРОГО одним JSON-объектом, без markdown-обёртки и без пояснений:
{"name": string, "brand": string|null, "net_weight": string|null, "kcal100": number|null, "p100": number|null, "c100": number|null, "f100": number|null, "per": "100g"|"portion"|"unknown", "portion_g": number|null, "basis": "label"|"estimate", "table_quote": string|null, "readable": true|false}

Действуй по порядку:

1. ЕСЛИ в кадре видна и читается таблица пищевой ценности — возьми точные значения из неё и верни basis="label".
   ОБЯЗАТЕЛЬНО заполни table_quote — выпиши строки таблицы так, как они там написаны, вместе с подписями и единицами: «белки 3,2 г; жиры 3,2 г; углеводы 4,7 г; 60 ккал».
   Это не украшение: без цитаты ответ basis="label" не принимается. Прочитал таблицу — значит, можешь её процитировать.
   ВНИМАТЕЛЬНО СЛЕДИ ЗА СТРОКАМИ. Жиры и углеводы в таблицах стоят рядом и легко съезжают на строку. Прежде чем ответить, сверь: 4×белки + 4×углеводы + 9×жиры должно примерно равняться калорийности. Не сходится — перечитай таблицу, ты попал не в ту строку или не в тот столбец.
   Столбцов часто два — «в порции» и «в 100 г». Бери столбец НА 100 Г. Если в кадре только «на порцию», верни per="portion" и portion_g.

2. ЕСЛИ таблицы в кадре нет или её не разобрать, НО продукт узнаётся по названию, марке, оформлению упаковки — верни название и марку, а КБЖУ дай ТИПИЧНЫЕ для ИМЕННО ЭТОГО продукта из своих знаний, basis="estimate", table_quote=null.
   Снимок лицевой стороны — это НЕ таблица. Не выдавай угаданные числа за прочитанные: basis="label" ставится только когда таблица реально в кадре.
   - Оценивай конкретный товар, а не категорию: для «Активиа черника 2.9%» — значения этого йогурта, а не «йогурта вообще». Учитывай жирность и вариант, если они написаны на упаковке.
   - Если продукт слишком нишевый и уверенных значений нет — оставь kcal100/p100/c100/f100 равными null, но название и марку всё равно верни, basis="estimate". Числа впишет человек.

3. readable=false — если на фото не удалось прочитать даже название продукта: кадр размыт, пустой или однотонный, упаковки в кадре нет, снят посторонний предмет. Ничего не угадывай: пустой кадр — это отсутствие данных.

Поля:
- name — ПОЛНОЕ название с пачки, ПО-РУССКИ. Это самое важное поле: по нему продукт потом отличают от соседей по полке, и его человек видит у себя в дневнике.
  ЯЗЫК. Название пиши по-русски. Есть русская сторона упаковки или русский стикер импортёра — бери оттуда дословно. Нет — переведи: «Maïs sans sucres ajoutés» → «Кукуруза сладкая без сахара», «Naturally Brewed Soy Sauce» → «Соевый соус натурального брожения». МАРКУ (brand) НЕ переводи и не транслитерируй: Pringles, Kikkoman, Bombbar так и остаются латиницей.
  ВКУС, ЛИНЕЙКА И ЖИРНОСТЬ — ЧАСТЬ НАЗВАНИЯ, а не деталь и не украшение. Выброси их — и разные товары станут неотличимы. ВЕС в name писать НЕ надо: для него отдельное поле net_weight, его допишут без тебя.
  Правильно: «VITAMIN зефир с кусочками брусники», «Активиа черника 2.9%», «Чипсы Original», «Соевый соус натурального брожения».
  Неправильно: «Зефир с кусочками брусники» (у той же марки есть зефир с другими ягодами — их не различить), «Активиа» (вкусов десяток), «Original» (нет вида продукта), «Naturally Brewed Soy Sauce» (не по-русски).
  Что НЕ берём: КАПС (пиши обычными буквами), «БЗМЖ», «ГОСТ», «ТУ», «новинка», «premium», «без ГМО», рекламные обещания.
- brand — торговая марка отдельно от названия («Простоквашино», «NEO botanica», «Домик в деревне»). Если марки не видно — null. Марку в name не дублируй.
- net_weight — МАССА НЕТТО ИЛИ ОБЪЁМ, как напечатано на упаковке, одной короткой строкой: «255 г», «950 мл», «0,5 л», «5 × 80 г», «12 шт».
  Ищи его специально: обычно внизу лицевой стороны, некрупно, иногда рядом со знаком EAC или в углу. Это отдельный вопрос, а не часть названия — ответь на него отдельно.
  Пиши ТОЛЬКО то, что видишь напечатанным. Не видно, закрыто пальцем, обрезано кадром — null. Не считай вес по размеру пачки и не бери его из своих знаний о товаре.
  Не путай с массой порции из таблицы («в 60 г») и с количеством в мультиупаковке без единиц.
- kcal100/p100/c100/f100 — калорийность (ккал), белки, углеводы, жиры (граммы).
- per — на какой вес приведены числа: "100g" — на 100 г продукта, "portion" — на порцию/упаковку, "unknown" — не указано. При basis="estimate" давай значения на 100 г и per="100g".
- portion_g — вес порции в граммах, если per="portion" и вес указан. Иначе null.
- table_quote — только при basis="label": строки таблицы дословно, с подписями и единицами. При basis="estimate" — null.
- При basis="label" числа приводи как есть с этикетки, НИЧЕГО не пересчитывай сам.`

// Промт ШАГА 2. Картинки тут нет: модель ищет по названию, которое шаг 1 уже
// прочитал с упаковки.
//
// name и brand приходят из ответа модели на шаге 1, то есть это НЕ ввод
// пользователя, но и не наш текст. Через cleanText они уже прошли обрезку по
// длине и схлопывание пробелов; подменить инструкцию через них нельзя — весь
// разбор ответа держится на JSON, а не на том, что модель напишет прозой.
// Штрих-код подставляется как есть: parseLabelInput пропускает дальше только
// 8–14 цифр.
const searchPrompt = ({ name, brand, barcode }) => `Нужна пищевая ценность на 100 г для конкретного товара из российского магазина.

Товар (прочитан с упаковки): ${brand ? `${brand} ${name}` : name}
Штрих-код: ${barcode}

Найди страницу ИМЕННО ЭТОГО товара поиском (инструмент web_search) и возьми КБЖУ оттуда.

- Запрос строй из марки и названия плюс «пищевая ценность на 100 г». Пример: «Шармэль зефир ванильный пищевая ценность на 100 г».
- Не нашлось с первого раза — уточни запрос штрих-кодом, чтобы среди похожих выбрать ровно тот товар.
- Годится только страница товара в магазине. Подборки, статьи, рецепты и «похожие товары» — нет.
- ПРОВЕРЬ, ЧТО ЭТО ТОТ САМЫЙ ВАРИАНТ: вкус, жирность, «классический/лёгкий», вес упаковки должны совпасть с названием выше. «Зефир ванильный» и «зефир в шоколаде» — разные товары с разными числами. Не совпало — это не тот продукт.

Ответь СТРОГО одним JSON-объектом, без markdown-обёртки и без пояснений после него:
{"found": true|false, "kcal100": number|null, "p100": number|null, "c100": number|null, "f100": number|null, "src_url": string|null}

- found=true — только если нашёл страницу этого товара и взял числа с неё. Тогда src_url — прямая ссылка на неё.
- found=false — если подходящей страницы нет или ты не уверен, что это тот же товар. Тогда все числа и src_url = null. Ответить «не нашёл» — нормально и правильно; выдумывать числа или прикладывать ссылку на похожий товар нельзя.
- Числа — ровно как на найденной странице, на 100 г, ничего не пересчитывай сам.`

// Разбор и проверка входа — ОТДЕЛЬНО от самого распознавания, потому что
// вызывается РАНЬШЕ учёта квоты. Иначе кривой запрос (нет фото, битый
// штрих-код) сжигал бы бесплатному пользователю одну из трёх суточных попыток,
// ничего ему не дав.
// Возвращает либо {status, error} — ответить и выйти, либо {barcode, image}.
function parseLabelInput(req) {
  const barcode = String(req.body?.barcode ?? '').trim()
  if (!isValidBarcode(barcode)) {
    return { status: 400, error: 'Штрих-код должен состоять из 8–14 цифр' }
  }

  // data:image/jpeg;base64,XXXX — клиент может прислать с префиксом (так
  // отдаёт canvas.toDataURL), Anthropic ждёт голый base64.
  const rawImage = typeof req.body?.image === 'string' ? req.body.image : ''
  const image = rawImage.includes(',') ? rawImage.slice(rawImage.indexOf(',') + 1) : rawImage
  if (!image) return { status: 400, error: 'Не приложено фото' }
  if (image.length > LABEL_MAX_BASE64) {
    // 413, а не 400: клиент по этому коду понимает, что дело именно в размере,
    // и может пережать кадр, а не сдаваться.
    return { status: 413, error: 'Фото слишком большое, пересними' }
  }
  return { barcode, image }
}

// Карточка на этот штрих-код уже есть в общем справочнике — и она не оценка?
// Тогда искать в интернете нечего, товар известен.
//
// Практически это подстраховка, а не основной механизм: фото-режим в
// BarcodeScanner открывается ТОЛЬКО из состояния «продукт не найден», то есть
// после промаха и по кэшу, и по Open Food Facts. Но ручка публичная, и клиент
// (свой будущий, чужой самописный) волен постучаться в неё напрямую — тогда
// без этой проверки мы платили бы за поиск того, что лежит у нас в базе.
//
// Любая ошибка чтения — false, и ОБЯЗАТЕЛЬНО через try/catch, а не только по
// полю error: справочник тут — оптимизация расхода, а не условие работы.
// Упавший запрос к базе не должен уронить распознавание, за которое человек
// уже отдал одну из своих суточных попыток.
async function alreadyKnown(supabaseAdmin, barcode) {
  const row = await existingCard(supabaseAdmin, barcode)
  return !!row && !isSoftSource(row.source)
}

// Карточка на этот штрих-код, какая бы ни была. Нужна двум местам: проверке
// «искать ли в интернете» выше и спасению прежних цифр ниже.
async function existingCard(supabaseAdmin, barcode) {
  if (!supabaseAdmin) return null
  try {
    const { data, error } = await supabaseAdmin
      .from('food_products')
      .select('barcode,name,brand,kcal100,p100,c100,f100,source')
      .eq('barcode', barcode).maybeSingle()
    return error ? null : data
  } catch (e) {
    console.error(`Этикетка ${barcode}: не прочитать справочник:`, e?.message)
    return null
  }
}

// Один запрос к модели: общий код обоих шагов. Своя причина существования —
// таймаут, разбор JSON из многоблочного ответа и логирование обрыва одинаковы
// и для зрения, и для поиска, а расходятся шаги только телом запроса.
//
// Возвращает { parsed, searches } либо null, если запрос не удался, — тогда
// вызывающий решает, отдавать 503 или обойтись без этого шага.
async function askModel(body, { label, barcode, userId }) {
  const controller = new AbortController()
  const timeoutId = setTimeout(() => controller.abort(), ANTHROPIC_TIMEOUT_MS)
  let data
  try {
    const response = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'x-api-key': process.env.ANTHROPIC_API_KEY,
        'anthropic-version': '2023-06-01',
      },
      body: JSON.stringify(body),
      signal: controller.signal,
    })
    if (!response.ok) {
      const detail = await response.text().catch(() => '')
      reportError('api:chat:label', [`Этикетка ${barcode} (${userId}): ${label} — Anthropic ответил ${response.status}: ${detail.slice(0, 300)}`],
        { message: `Anthropic ${response.status} (${label})`, status: response.status, userId })
      return null
    }
    data = await response.json()
  } catch (err) {
    reportError('api:chat:label', [`Этикетка ${barcode} (${userId}): ${label} — запрос к Anthropic не удался:`, err],
      { message: `Anthropic недоступен (${label}): ${err?.message}`, userId })
    return null
  } finally {
    clearTimeout(timeoutId)
  }

  const blocks = Array.isArray(data?.content) ? data.content : []
  const texts = blocks.filter(b => b?.type === 'text').map(b => b.text || '')

  // Не «первый блок» и не «последний»: и то и другое — догадка о том, куда
  // модель положит JSON. В живой проверке шага с поиском она вернула девять
  // блоков, из них пять текстовых подряд — комментарий, что нашла, ссылка,
  // разделитель, и только потом JSON. Идём с конца и берём первый, который
  // разобрался: так хвостовая приписка после JSON не превращает успешный
  // ответ в «не разобрано».
  let parsed = null
  for (let i = texts.length - 1; i >= 0 && !parsed; i--) parsed = parseModelJson(texts[i])

  const searches = blocks.filter(b => b?.type === 'server_tool_use' && b?.name === 'web_search').length

  // Обрыв уйдёт в общий отказ (JSON не разберётся), но причина у него другая,
  // чем «плохое фото», и в логе их надо различать: max_tokens — мал потолок,
  // pause_turn — модель не уложилась в цикл серверных инструментов.
  if (data?.stop_reason === 'max_tokens' || data?.stop_reason === 'pause_turn') {
    console.error(`Этикетка ${barcode} (${userId}): ${label} — ответ оборван, stop_reason=${data.stop_reason}, поисков=${searches}`)
  }
  return { parsed, searches }
}

// ШАГ 2 — уточнение чисел поиском. Отдельная функция, потому что её результат
// НЕОБЯЗАТЕЛЕН: не нашлось, отвалилась сеть, модель ответила мусором — карточка
// шага 1 остаётся в силе и уезжает к человеку как прикидка. Ни один исход
// шага 2 не должен превратиться в отказ распознавания.
//
// Возвращает { kcal100, p100, c100, f100, sourceName, sourceUrl } либо null.
async function searchNutrition({ product, barcode, userId }) {
  const got = await askModel({
    model: MODEL,
    max_tokens: LABEL_MAX_TOKENS_SEARCH,
    tools: LABEL_TOOLS,
    messages: [{
      role: 'user',
      content: [{ type: 'text', text: searchPrompt({ name: product.name, brand: product.brand, barcode }) }],
    }],
  }, { label: 'поиск', barcode, userId })

  if (!got) return { searches: 0, found: null }
  const { parsed, searches } = got
  if (!parsed || parsed.found !== true) return { searches, found: null }

  // Ссылка станет кликабельной в интерфейсе, поэтому проверяется отдельно от
  // allowed_domains: там мы ограничили, ГДЕ искать, здесь — что модель
  // приписала к числам. Нет ссылки из белого списка — нет и находки: числа без
  // проверяемого источника ничем не лучше прикидки, которая уже есть.
  const link = cleanSourceLink(parsed.src_url, SEARCH_DOMAINS)
  if (!link) return { searches, found: null }

  // Те же пределы правдоподобия, что и везде: 5400 ккал/100 г из карточки
  // магазина ничем не лучше 5400 ккал/100 г из головы модели.
  const macros = sanitizeMacros(parsed)
  if (macros.kcal100 === null) return { searches, found: null }

  return { searches, found: { ...macros, sourceName: link.name, sourceUrl: link.url } }
}

async function handleFoodLabel(req, res, { userId, barcode, image, paid, supabaseAdmin, today }) {
  // ── ШАГ 1: зрение, без инструментов.
  // Ровно тот запрос, что работал до появления поиска. Он и решает, есть ли на
  // фото продукт вообще — см. большой комментарий у LABEL_PROMPT о том, почему
  // поиск нельзя давать в этом же запросе.
  const seen = await askModel({
    model: MODEL,
    max_tokens: LABEL_MAX_TOKENS,
    messages: [{
      role: 'user',
      content: [
        { type: 'image', source: { type: 'base64', media_type: 'image/jpeg', data: image } },
        { type: 'text', text: LABEL_PROMPT },
      ],
    }],
  }, { label: 'зрение', barcode, userId })

  if (!seen) {
    return res.status(503).json({ error: 'Распознавание сейчас недоступно, попробуй ещё раз через минуту' })
  }

  // ВАЛИДИРУЕМ ОТВЕТ МОДЕЛИ КАК ЛЮБОЙ ДРУГОЙ НЕДОВЕРЕННЫЙ ВВОД. Модель может
  // выдать 5400 ккал/100 г, отрицательный белок или JSON не той формы — эти
  // числа поедут в ОБЩИЙ справочник, из которого их потом будут брать все.
  const product = normalizeLabelProduct(barcode, seen.parsed)

  // НОВАЯ ПОПЫТКА НЕ ДАЛА ЦИФР — НО ПРЕЖНИЕ ЕСТЬ.
  //
  // Живой случай: у творожка «снимок таблицы» оказался фотографией открытой
  // пачки. Строгие проверки честно отвергли всё — и человек остался вообще без
  // цифр, хотя прикидка с лицевой стороны у него уже была. Терять то, что уже
  // получено, нельзя: неудачная пересъёмка не должна стоить дороже, чем ничего.
  //
  // Отдаём прежнюю карточку и прямо говорим, что это она, а не результат
  // съёмки. Условие широкое намеренно: и когда не разобралось вообще, и когда
  // разобралось, но без пригодных чисел.
  if (!product || !hasUsableMacros(product)) {
    const prev = await existingCard(supabaseAdmin, barcode)
    if (prev && hasUsableMacros(fromRow(prev))) {
      console.log(`Этикетка ${barcode} (${userId}): не разобрана, оставляем прежнюю карточку «${prev.name}»`)
      return res.status(200).json({
        ok: true,
        product: { ...fromRow(prev), basis: isSoftSource(prev.source) ? 'estimate' : 'label', per: '100g', macroIssue: null, tableQuote: null, sourceName: null, sourceUrl: null },
        keptPrevious: true,
      })
    }
  }

  if (!product) {
    // Один ответ на три случая — модель не разобрала ничего, названия нет,
    // вернула не JSON: пользователю во всех трёх нужно ровно одно и то же —
    // переснять упаковку целиком.
    console.log(`Этикетка ${barcode} (${userId}): не разобрана (readable/name/JSON)`)
    return res.status(200).json({ ok: false, reason: 'unreadable' })
  }

  // ── ШАГ 2: поиск. СЕЙЧАС ВЫКЛЮЧЕН — см. LABEL_WEB_SEARCH_ENABLED и замеры
  // при нём. При выключенном флаге до этой ветки дело не доходит вовсе, и
  // поведение ровно то, что было до появления поиска: таблица в кадре —
  // точные цифры с этикетки, только лицевая сторона — прикидка.
  //
  // Остальные условия остаются в силе на случай возврата:
  //
  // basis='label' сюда не попадает намеренно: таблица с упаковки в кадре —
  // главный источник, уточнять её карточкой магазина незачем и вредно.
  //
  // Поиск платный поверх и без того платного vision-запроса, поэтому он с
  // уровня ПРОФИТ (пробный период даёт тот же уровень, см. TRIAL_LEVEL в
  // _access.js). Бесплатному СТАРТу остаётся прежнее поведение: распознавание
  // работает, числа при нечитаемой таблице — честная прикидка.
  let searches = 0
  if (webSearchOn() && product.basis === 'estimate' && paid && !(await alreadyKnown(supabaseAdmin, barcode))) {
    const r = await searchNutrition({ product, barcode, userId })
    searches = r.searches
    if (r.found) {
      // Числа и источник заменяем, название и марку — НЕТ: их прочитали с
      // упаковки в руках, и они вернее того, как товар назван в магазине.
      product.kcal100 = r.found.kcal100
      product.p100 = r.found.p100
      product.c100 = r.found.c100
      product.f100 = r.found.f100
      product.per = '100g'
      product.basis = 'web'
      product.sourceName = r.found.sourceName
      product.sourceUrl = r.found.sourceUrl
    }
  }

  // Учёт расхода на поиск — тем же механизмом, что и снимки (атомарный
  // incr_feature_usage), но своим ключом. Считаем ПОСЛЕ ответа и по факту:
  // решает искать модель, а не мы, и списывать за намерение было бы неверно.
  //
  // Ошибку учёта только логируем: не отдавать человеку уже полученную и
  // оплаченную карточку из-за неудачной записи в счётчик — хуже, чем
  // недосчитаться строки в статистике.
  if (searches > 0 && supabaseAdmin && today) {
    const { error: searchUsageError } = await supabaseAdmin
      .rpc('incr_feature_usage', { uid: userId, d: today, k: USAGE_KEY_SEARCH })
    if (searchUsageError) {
      console.error(`Этикетка ${barcode} (${userId}): не учтён расход на поиск:`, searchUsageError)
    }
  }

  console.log(`Этикетка ${barcode} (${userId}): распознано «${product.name}», basis=${product.basis}, per=${product.per}, поисков=${searches}${product.sourceName ? `, источник=${product.sourceName}` : ''}`)
  return res.status(200).json({ ok: true, product })
}

export default async function handler(req, res) {
  res.setHeader('Access-Control-Allow-Origin', '*')
  res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS')
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization')
  if (req.method === 'OPTIONS') return res.status(200).end()
  if (!rateLimit(req, res, { name: 'chat', limit: 12 })) return

  // Без валидного Supabase-токена — эндпоинт был открытым анонимным прокси
  // к Anthropic на нашем ключе (любой посторонний мог гонять запросы за наш
  // счёт). Авторизация обязательна ДО обращения к Anthropic.
  const authHeader = req.headers.authorization || ''
  const token = authHeader.startsWith('Bearer ') ? authHeader.slice(7) : null
  if (!token) return res.status(401).json({ error: 'Требуется авторизация' })

  const { data, error: authError } = await supabase.auth.getUser(token)
  if (authError || !data?.user) return res.status(401).json({ error: 'Требуется авторизация' })

  // ── Гейт по пакету. Клиентской блокировки (AIAssistant.jsx) недостаточно:
  // без этой проверки любой авторизованный пользователь дёргает /api/chat
  // напрямую и гоняет Anthropic за наш счёт в обход тарифа.
  //
  // Профиль читаем service_role-ключом: под RLS анонимный клиент чужую строку
  // не увидит, а нам нужен гарантированный ответ по id из токена.
  const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY
  if (!serviceRoleKey) {
    // Fail closed: без ключа уровень не проверить, а пускать всех к платной
    // функции нельзя. Ошибка громкая — чинится настройкой переменной.
    reportError('api:chat:config', ['SUPABASE_SERVICE_ROLE_KEY не настроен — проверка пакета невозможна'],
      { message: 'SUPABASE_SERVICE_ROLE_KEY не настроен', status: 500 })
    return res.status(500).json({ error: 'Сервер не настроен' })
  }

  const supabaseAdmin = createClient(SUPABASE_URL, serviceRoleKey)
  const { data: profile, error: profileError } = await supabaseAdmin
    .from('profiles')
    .select('plan, plan_until, trial_until, role')
    .eq('id', data.user.id)
    .maybeSingle()
  if (profileError) {
    reportError('api:chat:profile', [`ИИ-ассистент ${data.user.id}: ошибка чтения пакета:`, profileError],
      { message: profileError?.message, status: 500, userId: data.user.id })
    return res.status(500).json({ error: 'Не удалось проверить доступ' })
  }
  const isFoodLabel = req.body?.type === 'food_label'
  const level = effectiveLevel(profile)

  // Гейт по пакету — ТОЛЬКО для обычного чата. Распознавание этикетки открыто
  // всем вошедшим: общий справочник продуктов наполняют все, и пользуются им
  // тоже все, так что запирать сбор данных за тариф — стрелять себе в ногу.
  // Расход при этом ограничен квотами ниже, а не тарифом.
  if (!isFoodLabel && level < AI_MIN_LEVEL) {
    return res.status(403).json({ error: 'ИИ-ассистент доступен в пакете ПРОФИТ' })
  }

  // Дата по МСК (UTC+3) — общая для обоих суточных счётчиков.
  const msk = new Date(Date.now() + 3 * 60 * 60 * 1000)
  const pad = n => String(n).padStart(2, '0')
  const today = `${msk.getUTCFullYear()}-${pad(msk.getUTCMonth() + 1)}-${pad(msk.getUTCDate())}`

  // ── Ветка распознавания этикетки: свой вход, свои квоты, СВОЙ счётчик.
  // Ключевое: incr_ai_usage тут не вызывается вовсе — разобранные этикетки
  // больше не съедают дневной лимит реплик ассистента (и наоборот).
  if (isFoodLabel) {
    // Проверка входа ДО учёта квоты: кривой запрос не должен стоить
    // бесплатному пользователю одной из трёх суточных попыток.
    const input = parseLabelInput(req)
    if (input.error) return res.status(input.status).json({ error: input.error })

    const paid = level >= AI_MIN_LEVEL

    // Тестовый бот прогонов. Полный прогон — это 26 распознаваний подряд, а
    // почасовые 20 растягивают его на два часа ожидания в 429. Поднимаем потолки
    // ТОЛЬКО для служебного домена: живого пользователя с почтой на
    // qa.fitproapp.ru не существует и появиться не может — домен наш.
    //
    // Почту берём из подписанного токена (data.user), а не из тела запроса:
    // подделать её клиент не может, иначе лимиты обходились бы одной строкой.
    const isBot = String(data.user?.email || '').toLowerCase().endsWith('@' + QA_DOMAIN)
    const hourLimit = isBot ? BOT_LABELS_PER_HOUR : PAID_LABELS_PER_HOUR
    const dayLimit = isBot ? BOT_LABELS_PER_DAY : (paid ? PAID_LABELS_PER_DAY : FREE_LABELS_PER_DAY)

    // Платному — сначала почасовой потолок. Он in-memory, то есть бесплатный
    // по стоимости: упёршись в него, до базы не идём вовсе. Считаем ПО
    // ПОЛЬЗОВАТЕЛЮ (id из подписанного токена), а не по IP — за одним
    // оператором связи сидят сотни людей, и IP-лимит бил бы по чужим.
    if (paid && !rateLimit(req, res, { name: 'food-label', limit: hourLimit, windowMs: 3_600_000, subject: data.user.id })) return

    // Суточная квота — у обоих тарифов, различается только потолком. Живёт в
    // базе (см. sql/2026-08-05_feature_usage.sql). Инкремент атомарный;
    // считаем и отклонённые попытки — иначе подбором можно было бы крутить
    // счётчик бесконечно, ничего не тратя.
    //
    // Счётчик у тарифов ОБЩИЙ (один kind), и это осознанно: если тариф истёк
    // посреди дня, уже потраченные за сегодня снимки продолжают считаться —
    // человек не получает свежие три штуки сверх сотни, которую уже разобрал.
    // В обратную сторону так же: доплатив днём, он не обнуляет расход.
    const { data: used, error: usageError } = await supabaseAdmin
      .rpc('incr_feature_usage', { uid: data.user.id, d: today, k: 'food_label' })
    if (usageError) {
      // Fail closed, как и у дневного лимита чата ниже: без учёта квота не
      // держится, а за каждый снимок платим мы. Молча пускать нельзя.
      console.error(`Этикетка ${data.user.id}: ошибка учёта суточной квоты:`, usageError)
      return res.status(500).json({ error: 'Не удалось проверить лимит' })
    }
    if (used > dayLimit) {
      return res.status(429).json(paid
        ? { error: 'Дневной лимит фото исчерпан, продолжим завтра', reason: REASON_DAILY }
        : { error: `Лимит ${FREE_LABELS_PER_DAY} фото в день исчерпан`, reason: REASON_FREE_DAILY })
    }

    // Распознавание собирает запрос к модели само (свой промт, картинка вместо
    // истории переписки) и наружу отдаёт не сырой ответ Anthropic, а
    // разобранную и проверенную карточку.
    return handleFoodLabel(req, res, {
      userId: data.user.id,
      barcode: input.barcode,
      image: input.image,
      // paid определён выше по effectiveLevel: ПРОФИТ и выше, включая пробный
      // период. От него зависит, поедет ли в запрос инструмент поиска.
      paid,
      supabaseAdmin,
      today,
    })
  }

  // ── Дальше только обычный чат. Ниже ничего не менялось.
  // Анти-абьюз: ручку нельзя использовать как безлимитный LLM-прокси.
  // 1) Ограничение размера входа: суммарная длина content всех messages.
  const msgsForSize = Array.isArray(req.body?.messages) ? req.body.messages : []
  const inputChars = msgsForSize.reduce((sum, m) => {
    const c = m?.content
    if (typeof c === 'string') return sum + c.length
    if (Array.isArray(c)) return sum + c.reduce((s, b) => s + (typeof b?.text === 'string' ? b.text.length : 0), 0)
    return sum
  }, 0)
  if (inputChars > 16000) return res.status(400).json({ error: 'Слишком длинный запрос' })

  // 2) Пер-юзер дневной лимит по дате МСК. Инкремент атомарный через
  // security-definer RPC; считаем и отклонённые лимитом попытки.
  const { data: usageCount, error: usageError } = await supabaseAdmin.rpc('incr_ai_usage', { uid: data.user.id, d: today })
  if (usageError) {
    console.error(`ИИ-ассистент ${data.user.id}: ошибка учёта лимита:`, usageError)
    return res.status(500).json({ error: 'Не удалось проверить лимит' })
  }
  if (usageCount > 40) {
    return res.status(429).json({ error: 'Достигнут дневной лимит запросов к ассистенту, попробуйте завтра' })
  }

  // Тело клиента не прокидываем целиком — только system/messages, реально
  // нужные для ответа. model и max_tokens задаём/клампим сами (см. выше).
  const { system, messages } = req.body || {}
  const maxTokens = Math.min(Number(req.body?.max_tokens) || 2048, MAX_TOKENS_CEILING)

  // Свой таймаут чуть меньше maxDuration: если Anthropic отвечает слишком долго
  // или сеть залипла, лучше вернуть внятную ошибку самим, чем дать платформе
  // прибить функцию и отдать клиенту пустой обрыв соединения.
  const controller = new AbortController()
  const timeoutId = setTimeout(() => controller.abort(), ANTHROPIC_TIMEOUT_MS)
  try {
    const response = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'x-api-key': process.env.ANTHROPIC_API_KEY,
        'anthropic-version': '2023-06-01'
      },
      body: JSON.stringify({ model: MODEL, max_tokens: maxTokens, system, messages }),
      signal: controller.signal
    })
    const responseData = await response.json()
    res.status(response.status).json(responseData)
  } catch (err) {
    reportError('api:chat:anthropic', [`ИИ-ассистент ${data.user.id}: запрос к Anthropic не удался:`, err],
      { message: `Anthropic недоступен: ${err?.message}`, status: 503, userId: data.user.id })
    res.status(503).json({ error: 'ИИ-ассистент сейчас перегружен, попробуй ещё раз через минуту' })
  } finally {
    clearTimeout(timeoutId)
  }
}
