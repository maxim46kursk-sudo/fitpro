-- Дата старта «Потока 1»: 10 сентября 2026.
--
-- Зачем отдельным файлом. Дата — не настройка кода, а обещание участникам, и
-- жить она обязана там же, где цена и призовой фонд: в строке сезона. На
-- странице челленджа она НЕ ЗАШИТА нигде — ни в плашке шапки, ни в обратном
-- отсчёте комнаты, ни в подписи под кнопкой: всё читается из starts_on. Значит
-- перенос старта — это одна правка здесь, а не поиск числа по разметке.
--
-- Статус остаётся draft намеренно: набор ещё не открыт, билет живьём не
-- проверен. Дата от статуса не зависит — её объявляют раньше, чем начинают
-- продавать.
update public.challenge_seasons
   set starts_on = date '2026-09-10'
 where title = 'Поток 1';

select id, title, starts_on, status, price_rub
  from public.challenge_seasons
 order by id;
